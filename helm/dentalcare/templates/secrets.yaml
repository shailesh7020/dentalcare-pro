apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dentalcare.fullname" . }}-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://postgres:SuperSecretProdPass123!@dentalcare-postgres.internal:5432/dentalcare"
  REDIS_URL: "rediss://:RedisStrongProdAuth99!@dentalcare-redis.internal:6379/0"
  SECRET_KEY: "prod-secret-key-must-be-at-least-thirty-two-chars-long!"
  JWT_SECRET: "prod-jwt-secret-key-must-be-at-least-thirty-two-chars!"
